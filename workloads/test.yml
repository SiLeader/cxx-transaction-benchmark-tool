name: test
threads: 16
count: 1000

transaction:
  queries:
    - INSERT INTO txbench.bench(pk, field1, field2, field3) VALUES ('{{ random_string(32) }}', {{ random_number(0, 500000) }}, {{ random_number(0, 100000000) }}, {{ random_number(5, 150000) }})
